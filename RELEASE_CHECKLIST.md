# Release Checklist

Status values: `проверено`, `ещё выполнить`, `требует уточнения`.

## Required artifacts

| Item | Status | Notes |
| --- | --- | --- |
| `agent.py` with `Agent.act(env)` | проверено | `local_eval.py` ran successfully on commit `4c3d89b55da3af3272ae285a01bc2360030c3bae`. |
| `submission.csv` | ещё выполнить | Must be generated by `python make_submission.py`; it was not generated in this QA task. |
| `requirements.txt` | требует уточнения | Required only if the final agent needs packages beyond the organizer environment. |
| README/approach description | требует уточнения | Optional according to the guide; confirm whether the team wants to include it. |

## Submission format

| Requirement | Status | Notes |
| --- | --- | --- |
| `agent.py` is submitted with the required public interface | проверено | The local evaluator imported and executed the agent. |
| `submission.csv` has the generated campaign table | ещё выполнить | Generate it from the final `agent.py`; do not hand-edit it. |
| Submission is reproducible from the submitted code | ещё выполнить | Run `make_submission.py`, then verify the generated file against a repeat run. |
| Campaign rows contain valid tariff/channel values and required fields | проверено | The local evaluation reported no dropped campaigns for invalid tariff or channel. |
| No secrets, API keys, or customer personal data are included | проверено | No secrets or customer-level data were added by this QA task; perform a final repository review before submission. |

## Verification commands

| Command | Status | Notes |
| --- | --- | --- |
| `python local_eval.py` | проверено | Exit code 0; net result `+2,083,490` on seed 42. |
| `python local_eval.py --runs 10` | проверено | Exit code 0; median `+2,708,803`, positive `10/10`. |
| `python make_submission.py` | ещё выполнить | Required to create `submission.csv`; deliberately not run in this task. |
| Re-run submission generation and compare output | ещё выполнить | Confirms reproducibility required by the guide. |

## Resource limits

| Limit | Status | Observed result |
| --- | --- | --- |
| Maximum 20 pilots | проверено | Single run: `20/20`. |
| Maximum 15,000 total contacts | проверено | Single run: `5,067`. |
| Maximum 100,000 total budget | проверено | Single run: `73,314`. |
| 1 to 10 final campaigns | проверено | Single run: `2`; the 22 total shown includes 20 pilots. |
| Maximum 5,000 customers per campaign | проверено | Largest observed final campaign: `1,720`. |
| No invalid-tariff or invalid-channel dropped campaigns | проверено | No dropped-campaign message appeared in the single-run output. |
| Limits across every seed in the 10-run series | требует уточнения | The series output contains net results only, so resource metrics per seed were not confirmed. |
| Runtime no longer than 10 minutes | требует уточнения | Not measured as a timed acceptance check in this report. |

## Acceptance and organizer review

| Requirement | Status | Notes |
| --- | --- | --- |
| Local mechanics and scoring behavior checked | проверено | `local_eval.py` uses the scoring code on the mock environment. |
| Hidden-environment profitability | требует уточнения | The guide explicitly says mock effects differ from judging effects; local profit is not a guarantee. |
| Pilot use and pilot-informed selection | проверено | Single run reports `20` pilots; code-level strategy review remains the team's responsibility. |
| Organizer reproducibility check | ещё выполнить | Organizers rerun the submitted agent and compare `submission.csv` with generated output. |
| Final organizer decision criterion | требует уточнения | The guide defines net ARPU gain and validation rules, but does not specify a separate acceptance threshold or tie-break policy. |

## Before submission

| Action for Наурызбай | Status | Notes |
| --- | --- | --- |
| Review this QA report and the unresolved checklist items | ещё выполнить | Especially the submission and per-seed resource checks. |
| Run `python make_submission.py` on the final agent | ещё выполнить | This creates the required `submission.csv`. |
| Verify `submission.csv` is reproducible and contains only valid campaign rows | ещё выполнить | Repeat generation and inspect the resulting columns and values. |
| Run the final local checks again after any agent change | ещё выполнить | Use both `python local_eval.py` and `python local_eval.py --runs 10`. |
| Confirm runtime, dependency, and secret handling | ещё выполнить | Keep fallback behavior for any optional LLM calls and do not commit secrets. |
| Commit and submit `agent.py`, `submission.csv`, and any needed `requirements.txt` | ещё выполнить | Do not treat this QA documentation commit as the submission itself. |

## QA handoff

- QA branch: `codex/qa-release-report`
- Agent commit checked: `4c3d89b55da3af3272ae285a01bc2360030c3bae`
- Documentation files prepared: `EVAL_REPORT.txt`, `RELEASE_CHECKLIST.md`
- Mock result: single net `+2,083,490`; 10-run median `+2,708,803`; positive `10/10`.
- Remaining before release: generate and verify `submission.csv`, then resolve the
  items marked `ещё выполнить` or `требует уточнения`.