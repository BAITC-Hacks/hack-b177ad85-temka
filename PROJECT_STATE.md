# Current state

- Цель: создать агента тарифных маркетинговых кампаний, который использует пилоты и возвращает до 10 валидных кампаний.
- Последний коммит: `bfffcce` - добавлены командные правила, GitHub-процесс, `PROJECT_STATE.md` и skill `hackathon-eval`.
- Готово: стартовый пакет, исходный шаблон в `agent.py`, базовый `submission.csv`, командные правила, GitHub-процесс и skill `hackathon-eval` уже находятся в `main` и `origin/main`.
- Следующая задача: Наурызбай реализует безопасный каркас пилотов в `agent.py`; Асан параллельно передаёт top-10--15 кандидатов из истории; Султанали фиксирует baseline и все последующие прогоны.
- Владелец следующей задачи: Наурызбай.
- Проверено: `python local_eval.py --runs 10` завершился успешно; исходный шаблон дал отрицательный результат на 10 из 10 локальных seed'ов (медиана `-357 948`). После изменений агента обязателен новый прогон.
- Ограничения: максимум 20 пилотов, 15 000 контактов, 100 000 у.е. бюджета, 10 кампаний, 5 000 клиентов на кампанию.
- Риски / известные проблемы: стратегия пока является шаблоном организаторов; скрытые эффекты судейства нельзя хардкодить. В `bfffcce` вместе с правилами закоммичена строка `a = 500` в `mock_environment.py`; до следующей задачи Наурызбаю нужно подтвердить, что она намеренная.
