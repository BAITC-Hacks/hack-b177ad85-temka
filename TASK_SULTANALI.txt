СУЛТАНАЛИ — ТЕСТИРОВЩИК И КОНТРОЛЬ СДАЧИ
========================================

ТВОЯ ЦЕЛЬ
---------
Независимо проверять версии agent.py и говорить команде простой итог:
работает или не работает, устойчиво или нет, какие лимиты нарушены.
Тебе не нужно писать алгоритм и исправлять agent.py.

РЕКОМЕНДУЕМАЯ МОДЕЛЬ CODEX
--------------------------
Основной выбор: gpt-6-luna, reasoning Medium.
Она подходит для запуска готовых команд, извлечения метрик и таблиц.

Если тест падает и нужно разобраться в traceback:
переключись на gpt-6-sol, reasoning Medium.

Astra для обычных прогонов не нужна. Используй её только по просьбе Наурызбая
для финального сложного ревью.

ВАЖНО
-----
Ты не меняешь agent.py, environment.py, mock_environment.py, scoring_core.py,
local_eval.py и данные. Не запускай make_submission.py, пока Наурызбай не скажет,
что версия финальная, потому что эта команда перезаписывает submission.csv.

КАК ПОЛУЧИТЬ ВЕРСИЮ НАУРЫЗБАЯ
-----------------------------
Когда Наурызбай отправит имя ветки и SHA:

git fetch origin
git switch codex/agent-pilots
git pull --ff-only origin codex/agent-pilots
git status
git log -1 --oneline

Убедись, что SHA совпадает с сообщением Наурызбая.

ГОТОВЫЙ ПРОМПТ ДЛЯ CODEX
-------------------------
Скопируй весь текст ниже в новый чат Codex:

Прочитай полностью AGENTS.md, PROJECT_STATE.md,
.codex/skills/hackathon-eval/SKILL.md, local_eval.py, make_submission.py,
environment.py и текущий agent.py.

Я Султанали, независимый тестировщик. Не изменяй код и данные.

Задача:
1. Запусти python local_eval.py.
2. Запусти python local_eval.py --runs 10.
3. Собери понятный отчёт: exit code, медиана, минимум, максимум,
   число положительных запусков из 10, число пилотов, контакты, бюджет,
   количество кампаний, отброшенные кампании и traceback при ошибке.
4. Проверь лимиты: максимум 20 пилотов, 15 000 контактов,
   100 000 бюджета, 10 кампаний, 5 000 клиентов на кампанию.
5. Не исправляй agent.py. Если есть ошибка, укажи файл, строку и полный текст.
6. Сохрани итог только в EVAL_REPORT.txt.

Вердикт должен быть одним из трёх:
- PASS: 10/10 запусков в плюс, нет ошибок и нарушений.
- UNSTABLE: код работает, но есть отрицательные seed'ы или большой разброс.
- FAIL: исключение, невалидные кампании или нарушение лимитов.

КОМАНДЫ ПРОВЕРКИ
----------------

python local_eval.py
python local_eval.py --runs 10

После этого отправь Наурызбаю содержимое EVAL_REPORT.txt и SHA тестируемого
коммита. Не коммить отчёт в main без просьбы Наурызбая.

ЕСЛИ ТЕСТ УПАЛ
--------------
1. Ничего не исправляй сам.
2. Скопируй traceback полностью.
3. Напиши, какая команда упала.
4. Переключи Codex на gpt-6-sol Medium и попроси только объяснить причину.
5. Отправь объяснение Наурызбаю.

ФИНАЛЬНАЯ ПРОВЕРКА
------------------
Только после команды Наурызбая:

python make_submission.py
git status

Проверь, что submission.csv существует, содержит от 1 до 10 строк кампаний,
имеет target_tariff и channel и был создан именно make_submission.py.
Затем сообщи Наурызбаю, что файл готов. Сам в main не пушь.
